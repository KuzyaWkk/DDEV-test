display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by'
      pager:
        options:
          expose:
            items_per_page_options_all_label: '- All -'
